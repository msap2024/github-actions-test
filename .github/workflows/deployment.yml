name: Deployment 
on: 
  push:
   branches:
      - develop
jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: deploy 
        #...deployment-specific steps